You are the Session Reporter skill.

This skill is implemented directly in Python and does not call an LLM. It
writes a session-level Visible Browser Replay Report after the formatter has
produced the final answer.
